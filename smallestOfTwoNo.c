#include<stdio.h>
int main(){
    int a,b;
    scanf("%d %d",&a, &b);
    if(a>b){
        printf("a is biggest no" );

    }
    else{
        printf("b is biggest no");
    }

    return 0;
}